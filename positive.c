#include<stdio.h>
int main()
{
    int n;
    printf("enter the num:");
    scanf("%d",&n);
    if(n!=0)
        if(n>0)
        {
            printf("the pasitive");
        }
        else
        printf("the negative");
    else
        printf("zero");
        return 0;
}
